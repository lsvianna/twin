services:
  twin:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      GRADIO_SERVER_NAME: 0.0.0.0
      GRADIO_SERVER_PORT: 7860
    ports:
      - "7860:7860"
    restart: unless-stopped
