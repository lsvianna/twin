services:
  twin:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      GRADIO_SERVER_NAME: 0.0.0.0
      GRADIO_SERVER_PORT: 3000
    ports:
      - "3000:3000"
    restart: unless-stopped
